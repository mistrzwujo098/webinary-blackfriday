'use client'

/**
 * TrackingScripts - previously loaded FB Pixel, Google Ads, TikTok Pixel.
 * Now all tracking is managed by Cloudflare Zaraz (auto-injected on edge).
 */
export default function TrackingScripts() {
  return null
}
